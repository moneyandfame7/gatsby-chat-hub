/**
 *
 *  here switch global search and just conversation list
 * if focus in input then left column content=global search
 */

import React from 'react'

export const LeftMain: React.FC = () => {
  return <></>
}
